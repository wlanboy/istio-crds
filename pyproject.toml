[project]
name = "istio-crds"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "kubernetes>=36.0.3",
]

[dependency-groups]
dev = [
    "pyright>=1.1.411",
]
